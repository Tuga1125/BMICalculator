import React, { useState } from 'react';
import { Button, Text, TextInput, StyleSheet, View } from "react-native";

export default function BMI() {

    const [value, onChangeText] = useState('Enter your weight');

}